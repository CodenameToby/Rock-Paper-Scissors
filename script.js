// objects the user will be prompted to chose
// loop to run based on each potential user choice

window.alert("Do you want to play?") 
var rps = ["rock", "paper", "scissors"]
var userChoice = window.prompt("Enter rock, paper or scissors.")
userChoice = userChoice.toLowerCase;
console.log(userChoice)


function gamePlay( ){
    if (rps == "userChoice") {
        
    } elseif (rps == "userChoice"){
        
    } elseif (rps == "userChoice"){

    } else {
        console.log()
    }

}

gamePlay
